make the PRs visual, if we change something, screenshot it in all variations and comment in the PR with the images so we can get visal feedback

try to keep all the options available via get parameter

document all the get parameters in the readme and link some of them in matrixes so we can try different combinations
